server:
  port: 10086
spring:
  application:
    name: ly-registry
eureka:
  client:
    fetch-registry: false # 是否拉取注册信息
    register-with-eureka: false #是否注册导eureka
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
